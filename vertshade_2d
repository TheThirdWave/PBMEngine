#version 330 core

layout(location = 0) in vec3 position;
layout(location = 1) in vec3 color;

uniform mat4 MVPMat;

out vec2 Color;

void main()
{
        gl_Position = MVPMat * vec4(position, 1.0);
        Color = vec2(color.x, color.y);
}
