#version 330 core

out vec4 outColor;

in vec3 Color;
in vec3 Pos;

uniform sampler2D ourTexture;

void main()
{
        vec3 col = Color;
        col.z = col.z + 0.5;
        col.x = col.x / col.z;
        col.y = col.y / col.z;
        outColor = texture(ourTexture, col.xy);
}
